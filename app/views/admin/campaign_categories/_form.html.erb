<%= form_for(@campaign_category) do |f| %>
  <% if @campaign_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign_category.errors.count, "error") %> prohibited this campaign_category from being saved:</h2>

      <ul>
      <% @campaign_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :slug %><br />
    <%= f.text_field :slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
